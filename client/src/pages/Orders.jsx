import {useEffect,useState} from "react";import Navbar from "../components/Navbar";import {api,getToken} from "../services/api";
const stages=["Order Received","In Kitchen","Sent to Delivery"];
export default function Orders(){const [orders,setOrders]=useState([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{const get=async()=>{try{setOrders(await api("/orders",{token:getToken()}));setError("");}catch(e){setError(e.message);}finally{setLoading(false);}};get();const id=setInterval(get,15000);return()=>clearInterval(id);},[]);return <><Navbar/><main className="dashboard"><div className="dashboard-top"><div><span>MY ORDERS</span><h1>Track your pizzas</h1><p>Status refreshes automatically every 15 seconds.</p></div></div>{loading?<p>Loading orders…</p>:error?<div className="empty-orders"><h2>{error}</h2><a href="/login" className="primary-btn">Log in</a></div>:orders.length? <div className="orders-list">{orders.map(o=><article className="order-card" key={o._id}><div><strong>#{o._id.slice(-6).toUpperCase()}</strong><span className={`status ${o.paymentStatus}`}>{o.paymentStatus}</span><p>{o.pizzaBase.name} · {o.sauce.name} · {o.cheese.name}{o.vegetables.length?` · ${o.vegetables.map(x=>x.name).join(", ")}`:""}</p><b>₹{o.totalAmount}</b></div><div className="tracking">{stages.map((s,i)=><div className={stages.indexOf(o.orderStatus)>=i?"active":""} key={s}><i>{i+1}</i><small>{s}</small></div>)}</div></article>)}</div>:<div className="empty-orders"><div>🍕</div><h2>No orders yet</h2><p>Build your first pizza and it will appear here.</p><a href="/builder" className="primary-btn">Build My Pizza</a></div>}</main></>}
