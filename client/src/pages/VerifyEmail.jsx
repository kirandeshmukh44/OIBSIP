import {useEffect,useState} from "react";import {useSearchParams} from "react-router-dom";import {api} from "../services/api";export default function VerifyEmail(){const [p]=useSearchParams(),[message,setMessage]=useState("Verifying your email…");useEffect(()=>{api("/auth/verify-email",{method:"POST",body:JSON.stringify({token:p.get("token")})}).then(x=>setMessage(x.message)).catch(e=>setMessage(e.message));},[p]);return <div className="auth-page"><div className="auth-card"><div className="auth-logo">🍕</div><h1>Email verification</h1><p>{message}</p><a className="primary-btn" href="/login">Go to login</a></div></div>}
