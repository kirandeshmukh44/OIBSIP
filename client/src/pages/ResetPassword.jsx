import {useState} from "react";import {useSearchParams,useNavigate} from "react-router-dom";import {api} from "../services/api";export default function ResetPassword(){const [p]=useSearchParams(),nav=useNavigate(),[password,setPassword]=useState(""),[message,setMessage]=useState("");const submit=async e=>{e.preventDefault();try{setMessage((await api("/auth/reset-password",{method:"POST",body:JSON.stringify({token:p.get("token"),password})})).message);setTimeout(()=>nav("/login"),1200);}catch(x){setMessage(x.message);}};return <div className="auth-page"><div className="auth-card"><h1>Choose a new password</h1><form onSubmit={submit}><label>New password</label><input required minLength="8" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="primary-btn">Update password</button></form>{message&&<p className="form-message">{message}</p>}</div></div>}
