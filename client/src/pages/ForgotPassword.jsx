import {useState} from "react";import {api} from "../services/api";export default function ForgotPassword(){const [email,setEmail]=useState(""),[message,setMessage]=useState("");const send=async e=>{e.preventDefault();try{setMessage((await api("/auth/forgot-password",{method:"POST",body:JSON.stringify({email})})).message);}catch(err){setMessage(err.message);}};return <div className="auth-page"><div className="auth-card"><div className="auth-logo">🍕</div><h1>Reset password</h1><p>We’ll email you a secure reset link.</p><form onSubmit={send}><label>Email</label><input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="primary-btn">Send reset link</button></form>{message&&<p className="form-message">{message}</p>}</div></div>}
