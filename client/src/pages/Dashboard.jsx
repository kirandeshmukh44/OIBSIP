import {useEffect,useState} from "react";import Navbar from "../components/Navbar";import {api,getToken} from "../services/api";
export default function Dashboard(){const [name,setName]=useState("");useEffect(()=>{api("/auth/profile",{token:getToken()}).then(x=>setName(x.user.name)).catch(()=>{});},[]);const logout=()=>{localStorage.removeItem("token");localStorage.removeItem("user");window.location.href="/login";};return <><Navbar/><main className="dashboard"><div className="dashboard-top"><div><span>MY ACCOUNT</span><h1>Welcome{name?`, ${name}`:""} 🍕</h1><p>Ready to build your next pizza?</p></div><button className="secondary-btn" onClick={logout}>Logout</button></div><div className="dashboard-grid"><div className="dashboard-card"><span>🍕</span><h2>Build a Pizza</h2><p>Create your own custom pizza.</p><a href="/builder" className="primary-btn">Start Building</a></div><div className="dashboard-card"><span>📦</span><h2>My Orders</h2><p>Check current order status and order history.</p><a href="/orders" className="secondary-btn">View Orders</a></div></div></main></>}
