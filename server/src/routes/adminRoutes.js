const router=require("express").Router(), protect=require("../middleware/adminMiddleware"),c=require("../controllers/adminController");router.post("/login",c.login);router.use(protect);router.get("/dashboard",c.dashboard);router.get("/orders",c.orders);router.patch("/orders/:id/status",c.changeStatus);router.route("/inventory").get(c.inventory).post(c.addInventory);router.patch("/inventory/:id",c.updateInventory);module.exports=router;
